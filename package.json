{
  "name": "devmentor-monorepo-proxy",
  "private": true,
  "scripts": {
    "start": "cd backend && npm ci && npm start",
    "build": "cd backend && npm run build || true"
  },
  "engines": {
    "node": ">=18"
  }
}
